# clang-format 설정 파일 (Windows)
# 줄 앞뒤 공백은 무시됩니다. # 으로 시작하는 줄은 주석입니다.
# 쉼표(,)로 여러 디렉터리를 구분합니다.

# 절대 경로 권장. 비워두면 PATH에서 검색합니다.
CLANG_FORMAT_PATH=C:\LLVM\21.x\bin\clang-format.exe

# 포맷 대상 디렉터리 목록 (workspace 루트 기준)
TARGET_DIRS=CommonUtils,lv00,lv01,lv02,lv03

# 포맷 대상 파일 형식
TARGET_EXTS=c,cpp,h